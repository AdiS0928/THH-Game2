<!DOCTYPE html>
<html>
    <head>
      <link rel="stylesheet" href="css/leaderboard.css">
    </head>

    <body>
        <div class="back">
            <div>
                <p class="nametxt"><%= usera %></p>
                <p class="nametxt">NICE WORK,</p>
            </div>

                <div class="scorecontain">
                    <p class="scorehead">YOUR SCORE</p>
                    <p class="score"><%= scorea*3 %></p>
                </div>


            <div>
                <table class="table">
                    <tr>
                        <td>Name</td>
                        <td>Scores</td>
                    </tr>
                    
                    <%moviesList.forEach(movie => {%>
                        <tr>
                            <td><%= movie.User %></td>
                            <td><%= movie.Score*3 %> </td>
                        </tr>
                    <%})%>

                </table>
            </div>
            <form action="/" method="get" class="pam">
            <button class='card-button' type="submit">
            <span class="txt2"> PLAY AGAIN </span>
            </button>

                <!-- <p class="txt3">MAJID</p> -->

        </form>
        </div>

        <script>
            var w = window.innerWidth;
var h = window.innerHeight;
            if(w<1000){
                const element = document.querySelector('.table');
        element.style.height = '40rem';
        element.style.width = '40rem';
        element.style.fontSize = '2.5rem';

        const element2 = document.querySelector('.nametxt');
        element2.style.fontSize = '5rem';
            }
          </script>
          
    </body>
</html>